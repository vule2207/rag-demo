services:
  rag-chatbot:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: rag_chatbot_app
    env_file:
      - .env
    volumes:
      # Map local papers directory to container
      # Allows adding new PDFs without rebuilding the image
      - ./papers:/app/papers
      # Optional: Map main.py for hot-reloading during development
      - ./main.py:/app/main.py
    # Optimization: Use init process to handle signals like Ctrl+C properly
    init: true
    # Keep standard input open and allocate a pseudo-TTY for CLI interaction
    stdin_open: true
    tty: true